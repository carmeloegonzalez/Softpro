@model IEnumerable<Softpro.Common.Entities.Adm_Subgrupos>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Subgrupo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Subgrupo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SubgrupoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SubgrupoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SubgrupoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
